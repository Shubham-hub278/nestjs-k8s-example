apiVersion: v1
kind: Service
metadata:
  name: k8s-demo
  labels:
    app: k8s-demo
spec:
  type: NodePort
  selector:
    app: k8s-demo
  ports:
    - name: http
      port: 3000
      targetPort: 3000
